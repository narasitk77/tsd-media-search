node_modules/
.env
data/sessions.db
data/activity.db
*.log
.DS_Store

# runtime user data — never commit
data/users.json

# migration export data — keep out of git (large + may be sensitive)
migration/out/
